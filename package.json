{
  "name": "@unllamas/component-test",
  "version": "0.0.2",
  "private": false,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "commit": "sui-mono commit",
    "prepare": "pnpm lint && pnpm build && pnpm format"
  },
  "devDependencies": {
    "@unllamas/eslint-config": "workspace:*",
    "@unllamas/typescript-config": "workspace:*",
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@s-ui/mono": "^2.42.0"
  }
}
